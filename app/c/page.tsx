// This file exists for the /c route, but the layout handles redirecting to /c/dashboard
// The redirect happens at the layout level to avoid conflicts with server actions
export default function ClientRootPage() {
  return null;
}


